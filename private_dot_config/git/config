[user]
	signingkey = /home/kerberos/.ssh/id_ed25519_signing
	email = 38499069+niri81@users.noreply.github.com
	name = niri81
[commit]
	gpgsign = true
[init]
	defaultBranch = main
[gpg]
	format = ssh

[core]
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true  # use n and N to move between diff sections
  dark = true      # or light = true, or omit for auto-detection

[merge]
	conflictStyle = zdiff3

[rerere]
	enabled = true   # reuse recorded resolutions (solve repeating conflicts automatically)
